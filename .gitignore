# Xcode
*.xcuserstate
xcuserdata/
*.xccheckout
*.moved-aside
DerivedData/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# CocoaPods
Pods/
*.xcworkspace

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Build artifacts
build/
*.o
*.d

# Local API keys — never commit. Optional: the app works without it, each user
# enters their own xeno-canto key in Settings. Delete before publishing.
BirdApp/DeveloperKeys.plist
